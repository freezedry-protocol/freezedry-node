# Pre-commit blocklist and gitleaks config — contain actual wallet/IP patterns
.githooks/.blocklist
.gitleaks.toml

node_modules/
db/*.db
db/*.db-wal
db/*.db-shm
blobs/
.env
.env.*
.env.local
.env*.local
*.log
*-keypair.json
*.pem
secrets/
credentials.*
keys/

# OS junk
.DS_Store
Thumbs.db

# Admin scripts — use env vars, not safe to share
update-config.mjs

# Deploy/ops — SSH creds, server hostnames
deploy.sh
STATUS.md

# CLAUDE files — private project context
CLAUDE.md
**/CLAUDE.md

# Optimization harness output
scripts/results.jsonl
scripts/STOP

# Editor state
**/.obsidian/
